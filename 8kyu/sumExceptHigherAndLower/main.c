/*
Sum all the numbers of a given array ( cq. list ), except the highest and the lowest 
element ( by value, not by index! ).

The highest or lowest element respectively is a single element at each edge, 
even if there are more than one with the same value.

Mind the input validation.*/

#include <stdio.h>

int sum(const int numbers[/*length*/], int length)
{
  int lower = 9999, higher = 0, sumUseless = 0, sum = 0;

  for(int i = 0; i < lenght; i++){
    if(numbers[i] < numbers){
      
    }
    sum = numbers[i];
    i++;
    }
    return sum ;
}
